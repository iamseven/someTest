<%= form_for(@dapi) do |f| %>
  <% if @dapi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dapi.errors.count, "error") %> prohibited this dapi from being saved:</h2>

      <ul>
      <% @dapi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
